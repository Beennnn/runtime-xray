XR.bloc("src","lab/sample/terrain/Elevation.java",["package lab.sample.terrain;","","import lab.sample.model.Leg;","","/** Terrain along a leg, sampled into sub-segments."," *"," *  <p>Deterministic: a given leg's profile is always the same, otherwise two runs would no"," *  longer be comparable. */","public final class Elevation {","","    private Elevation() {}","","    /** @return the {@code step}-th sub-segment's gradient, as a percentage (−8 % to +8 %). */","    public static double gradePercentAt(Leg leg, int step) {","        // Plain arithmetic rather than Math.sin/cos: the trigonometric intrinsics showed up","        // as the profile's dominant leaves (dsin/dcos, ~50 % of the samples), which made the","        // call tree unreadable for anyone looking for the business methods. The terrain stays","        // deterministic and varied.","        double phase = (Math.abs(leg.from().hashCode()) % 17) + step;","        double wave = ((phase * 37) % 21) - 10.0;          // -10 .. +10, sawtooth","        double smooth = wave * (1.0 - Math.abs(wave) / 25.0);","        return smooth * 0.8;","    }","}"]);
XR.bloc("src","lab/sample/terrain/Terrain.java",["package lab.sample.terrain;","","import lab.sample.model.Leg;","import lab.sample.model.Mode;","","/**"," * Slowdown caused by the terrain, averaged over a leg's sub-segments."," *"," * <p>Two reasons to exist:"," * <ul>"," *   <li>it is the program's <b>computational core</b> — without it, the application spends"," *       its time in the JDK's iterators and a profiler shows nothing of the business;</li>"," *   <li>it is called only for the modes sensitive to terrain: the train follows a track"," *       whose gradient is negligible, so it skips this whole sub-tree.</li>"," * </ul>"," */","public final class Terrain {","","    /** Number of sub-segments analysed per leg. */","    private static final int STEPS = 24;","","    private Terrain() {}","","    public static boolean applies(Mode mode) {","        return mode != Mode.TRAIN;","    }","","    public static double slowdownFactor(Leg leg, Mode mode) {","        double sum = 0;","        for (int step = 0; step < STEPS; step++) {","            double grade = Elevation.gradePercentAt(leg, step);","            sum += penaltyFor(grade, mode);","        }","        return sum / STEPS;","    }","","    /** A climb costs more than a descent gives back, all the more so under one's own","     *  muscle power. */","    private static double penaltyFor(double gradePercent, Mode mode) {","        double sensitivity = switch (mode) {","            case BIKE -> 0.09;","            case WALK -> 0.05;","            case CAR -> 0.01;","            case TRAIN -> 0.00;","            case PLANE -> 0.00;","        };","        if (gradePercent > 0) {","            return 1.0 + gradePercent * sensitivity;","        }","        return 1.0 + gradePercent * sensitivity * 0.3; // going down only partly makes up for it","    }","}"]);
