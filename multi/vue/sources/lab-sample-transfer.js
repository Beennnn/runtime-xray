XR.bloc("src","lab/sample/transfer/Connections.java",["package lab.sample.transfer;","","import lab.sample.model.Leg;","","import java.util.List;","","/**"," * Waiting time at connections — specific to the train."," *"," * <p>The computation is <b>recursive</b> over the list of legs: it adds a recursion site"," * distinct from {@code comfort.Breaks}', in a branch only some scenarios take. Useful to"," * check that a tool does not confuse two different recursions in the same tree."," */","public final class Connections {","","    private Connections() {}","","    public static double waitMinutes(List<Leg> legs, int index) {","        if (index >= legs.size() - 1) {","            return 0; // last leg: no connection after it","        }","        double here = transferMinutes(legs.get(index), legs.get(index + 1));","        return here + waitMinutes(legs, index + 1);","    }","","    private static double transferMinutes(Leg arriving, Leg departing) {","        // A connection within the same station is faster than a change of station, and the","        // wait depends on the service frequency — hence on a timetable lookup, the program's","        // only blocking operation.","        double walk = arriving.to().equals(departing.from()) ? 6 : 18;","        return walk + Timetable.frequencyMinutes(departing) / 2.0;","    }","}"]);
XR.bloc("src","lab/sample/transfer/Timetable.java",["package lab.sample.transfer;","","import lab.sample.model.Leg;","","import java.util.Map;","import java.util.concurrent.ConcurrentHashMap;","","/**"," * Timetable lookup — the program's only <b>blocking</b> operation."," *"," * <p>It simulates a call to an external service with a {@code Thread.sleep}. Its presence is"," * not decorative: it creates <em>waiting time</em> alongside computation time, and that is an"," * excellent discriminator between tools. A profiler that samples the CPU will hardly see it;"," * a wall-clock profiler will see it dominate. Two tools can therefore give two opposite"," * answers to \"where does the time go?\" on exactly the same run — and one has to know which"," * one is being looked at."," *"," * <p>The result is cached: a timetable is asked for only once per link."," */","public final class Timetable {","","    private static final Map<String, Integer> CACHE = new ConcurrentHashMap<>();","    private static final int LOOKUP_MILLIS = 3;","","    private Timetable() {}","","    /** @return the service frequency, in minutes, for this link. */","    public static int frequencyMinutes(Leg leg) {","        return CACHE.computeIfAbsent(leg.from() + \">\" + leg.to(), key -> {","            try {","                Thread.sleep(LOOKUP_MILLIS); // simulated network call","            } catch (InterruptedException e) {","                Thread.currentThread().interrupt();","            }","            return 10 + (Math.abs(key.hashCode()) % 50);","        });","    }","}"]);
